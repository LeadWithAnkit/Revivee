import { Link, useParams } from "react-router-dom";
import { ArrowLeft, CheckCircle2 } from "lucide-react";
import { problems } from "../data/content";
import { Card, EvidenceBadge, SectionTitle } from "../components/ui";
export default function ProblemDetail(){const {id}=useParams();const p=problems.find(x=>x.id===id)||problems[0];return <div className="page"><Link to="/problems" className="back-link"><ArrowLeft size={15}/> Problem library</Link><SectionTitle eyebrow="Observation" title={p.title} body={p.description}/><div className="problem-detail-grid"><Card><span className="eyebrow">Possible contributors</span><div className="chip-list">{p.contributors.map(x=><span key={x}>{x}</span>)}</div><span className="eyebrow detail-label">Evidence framing</span><EvidenceBadge level="plausible"/><p className="muted">These are candidate relationships for observation, not conclusions about your individual case.</p></Card><Card><span className="eyebrow">What you can try</span><div className="try-list">{p.interventions.map((x,i)=><div key={x}><CheckCircle2 size={16}/><span><b>{i+1}. {x}</b><small>Record whether starting or continuing became easier.</small></span></div>)}</div></Card></div></div>}
